spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

xiaoc:
  queue:
    mode: local
  redis:
    enabled: false
    fail-open: true

logging:
  level:
    org.hibernate.SQL: warn
